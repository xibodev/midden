name: Stage core and bundles
on:
  push:
    branches: [staging, 'feat/core-bundle-*']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: ./.github/workflows/validate.yml
